/**
 * フロントエンドとバックエンドの両方から import される共有モジュール。
 *
 * ここに置くのは「両側が同じ形として合意しているもの」だけ:
 *   - リクエスト/レスポンスの zod スキーマと、そこから導出した型
 *   - ドメインの型
 *
 * ビルド成果物ではなく TS ソースをそのまま公開している（package.json の exports 参照）ので、
 * 編集すれば Vite / tsx 側に即座に反映される。ビルド順を気にする必要はない。
 */
export * from './contracts'
